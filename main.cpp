#include <iostream>


using namespace std;
/**
 * 1. pos 还剩下的元素的个数
 * 2. x 表示为不是数组的下标, 而是自然的下标
 *
 * 处理的部分: 所有的代码含有自己的main 函数进行处理
 *
 * 对于stack 的使用都是public 的, 所以 5 and 6 是没有区别的
 *
 *  所有的G 都是需要进行修改的
 *
 *  模拟队列的结果需要处理为：2 m
 *      2. push 的时候 大小检查
 *      3. 输出的 大小
 *  区分各自的大小 和 stack 的大小
 *
 *  I : 进入到s1 中间， 使用 full 检查的， 在外部
 *  full: s1 为full, and s2 is not empty !
 *  O: ok
 *
 *  寻址： s2 中间的编号的位置 在前面
 *
 *  don't use () if not necessary
 *
 *
 *  x :maybe misunderstood, yes is just the normal x
 *
 *  we only have one stack for three file
 * */




int main(int argc, char* argv[]){
     // 创建读入的标准模板实现


    return 0;
}

